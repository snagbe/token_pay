create database dbbank;

create table "user"
        (
        tag_id int
        constraint user_pk
        primary key,
        username varchar(20),
        name varchar(35),
        firstname varchar(35),
        budget decimal(10,2)
        birthday date
        );

//
INSERT INTO "public"."user" ("tag_id", "username", "name", "firstname", "budget", "birthday") VALUES (798488828269, 'user100', 'Hammer', 'Bruno', 100, '1982-03-17')
[2020-03-05 20:27:24] 1 row affected in 27 ms